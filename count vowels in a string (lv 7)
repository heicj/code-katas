level 7

```
def count_vowels(s = ''):
    # your code
    count = 0
    if s is int:
        return
    else:
        vowels = ['a','e','i','o','u']
        for letter in s:
           if letter is int:
              continue
           elif letter.lower() in vowels:
              
              count += 1
    return count
  ```
